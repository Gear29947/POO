console.log("vinculado correctamente")
// Ejercicio 1: Declaración de variables
// Instrucciones: Declara dos variables numéricas llamadas 'ahora' y 'fecha_nac'.
// Luego, calcula y muestra en un mensaje la edad actual basada en el año de nacimiento.

    
function declararVariables() {
    let ahora = 2024;
    let fecha_nac = 2004;
    let resultado = actual - nac;
    alert("Mi edad es: " + resultado);
    alert("Esto es una alerta")
    }
    
    // Ejercicio 2: Asignación de valores
    // Instrucciones: Completa la función para asignar el valor de 'var1' a 'var2' y luego muestra el valor de 'var2'.
    function asignarValores() {
    alert("var2 = 20")
    let (var1 = 24)
    let (var2 = 20)
    
    }
    
    // Ejercicio 3: Operaciones matemáticas
    // Instrucciones: Completa la función para realizar las siguientes operaciones:
    // Suma 'num1' y 'num2' y ´num3´, resta 'num1' de 'num3', multiplica 'num2' por 'num3', y divide 'num1' entre '120'.
    // Muestra los resultados en un mensaje.
    function realizarOperaciones() {
        let num1 = 20;
        let num2 = 40;
        let num3 = 2;
        let suma = num1 + num2 + num3;
        let resta = num1 - num3;
        let multi = num2 * num3;
        let divi = num1 / 120;
        alert(`la suma es: ${suma} \n  la resta es ${resta} \n la multi es ${multi}\n la divi es ${divi}`)
    // Tu código aquí
    }
    
    // Ejercicio 4: Manipulación de cadenas
    // Instrucciones: Completa la función para construir un mensaje utilizando las variables 'direcion' y 'n_casa'.
    // Luego, muestra la longitud del mensaje y el antepenuntimo en el mensaje.
        // Ejercicio 4: Manipulación de cadenas
// Instrucciones: Completa la función para construir un mensaje utilizando las variables 'nombre' y 'apellido'.
// Luego, muestra la longitud del mensaje y el primer carácter en el mensaje.
    function manipularCadenas(){
    let direccion = "La Pintana";
    let n_casa = "1820";
    let mensaje = "Mi direccion es : " + direccion + " " + n_casa;
    let longitud = mensaje.length;
    alert("Mensaje: " + mensaje +"\nLongitud: " + longitud +"\nPrimer Caracter: " + mensaje[0]);
    }
    function sumaVariable(){
    valor = [1,2,3,4,5];
    ultiValor =valor[valor.length-1];
    penultimoValor =valor[valor.length-2];
    primerValor =valor[0];
    suma= ultiValor + penultimoValor + primerValor;
    alert(`el resultado es : ${suma}`);
}